- set_meta_tags  title: 'ご依頼フォーム'
- set_meta_tags  description: ''

.tops-wrapper
  h2.headline
      | ご依頼フォーム
  =  form_for @contract, url: confirm_contracts_path(@contract) do |f|
    = render 'form', f: f, btn_name: "確認画面"
